# Qwen 2.5 0.5B Instruct — Mac M5 / MPS development model
# Pipeline validation only; not a primary result model.
hf_model_id: Qwen/Qwen2.5-0.5B-Instruct
hf_revision: 7ae557604adf67be50417f59c2c2f167def9a775
n_layers: 24
hidden_size: 896
torch_dtype: float16            # MPS prefers float16 over bfloat16
device_map: mps                 # Mac dev model: force MPS. `auto` resolves
                                # to CPU on Apple Silicon → float16 runs via
                                # slow emulation and generation crawls.
trust_remote_code: false
