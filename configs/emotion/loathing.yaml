name: loathing
label: 19                      # integer label used in probe training arrays
prompt_file: emotion_prompts.parquet
neutral_file: neutral_prompts.parquet
# Plutchik high-intensity disgust pole; opposite of `admiration`.
# Primary emotion (2026-06-12 amendment). Seed stimuli authored: 50 rows
# (emotion_label == "loathing") in emotion_prompts.parquet.
# Augmentation DEFERRED (may be unnecessary): LLM paraphrase is a prime
# suspect in the H1 AUC=1.0 confound (per-emotion style fingerprint), so
# run the H1 confound audit on these 50 seeds first. If more N is needed,
# prefer hand-authoring more seeds over LLM augmentation. See the
# 2026-06-12 amendment in HYPOTHESES.md.
