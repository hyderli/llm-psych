name: anger
label: 1                       # integer label used in probe training arrays
# prompt_file is relative to paths.data_dir in config.yaml
prompt_file: emotion_prompts.parquet
neutral_file: neutral_prompts.parquet
